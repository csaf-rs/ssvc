[package]
name = "ssvc"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
regress = "0.10"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
serde_json = "1"
typify = "0.6"
prettyplease = "0.2"
syn = "2"
